@model IEnumerable<Assignment.Model.National>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NatinalName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NatinalName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NationalId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NationalId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NationalId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
